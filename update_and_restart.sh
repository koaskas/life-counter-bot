#!/usr/bin/env bash
# Переходим в папку скрипта (гарантирует, что команда выполнится в правильном каталоге)
cd "$(dirname "$0")"

# 1. Подтягиваем последние изменения
sudo git pull origin main

# 2. Собираем и перезапускаем контейнер (если нужно — sudo)
sudo docker-compose up -d --build

echo "Обновление завершено: код свежий, контейнер перезапущен."
